{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug Rust",
            "cargo": {
                "args": ["build"]
            },
            "program": "${workspaceFolder}/target/debug/rust-donation-api",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}